import { ArrowRight, MapPin } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { GeoJsonVillageProperties, VillageRanking } from '../../types/api'

interface Props { village?: VillageRanking; location?: GeoJsonVillageProperties }
const value = (number: number | undefined, suffix = '') => number === undefined ? '—' : `${number.toLocaleString(undefined, { maximumFractionDigits: 1 })}${suffix}`
export function SelectedVillagePanel({ village, location }: Props) { if (!village) return <aside className="flex min-h-56 flex-col justify-center border-t border-[#e5e4de] bg-[#fffefa] p-6 lg:border-l lg:border-t-0"><p className="text-lg font-semibold text-[#172033]">No village selected</p><p className="mt-2 max-w-xs text-sm leading-6 text-slate-500">Select a village on the map to explore its available solar and investment information.</p></aside>; return <aside className="border-t border-[#e5e4de] bg-[#fffefa] p-5 lg:border-l lg:border-t-0"><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#a4771b]">Selected village analysis</p><h2 className="mt-1 text-xl font-semibold text-[#172033]">{village.village_name}</h2><p className="mt-1 flex items-center gap-1 text-sm text-slate-500"><MapPin size={14} />{village.village_id}{location?.district ? ` · ${location.district}` : ''}</p><div className="mt-5 border-y border-[#ecebe6] py-4"><p className="text-xs text-slate-500">Priority Rank</p><p className="mt-1 text-2xl font-semibold text-[#8a620f]">#{value(village.rank)}</p></div><div className="grid grid-cols-2 gap-x-4 gap-y-4 py-4 text-sm"><Metric label="Investment Score" value={value(village.investment_score)} /><Metric label="Annual Generation" value={value(village.annual_generation_mwh, ' MWh')} /><Metric label="ROI" value={value(village.roi_percent, '%')} /><Metric label="Payback" value={value(village.payback_years, ' years')} /><Metric label="Grid Distance" value={value(village.grid_distance_km, ' km')} /></div><Link to={`/village-intelligence/${village.village_id}`} className="mt-2 inline-flex items-center gap-2 text-sm font-semibold text-[#8a620f] hover:text-[#674807]">View Village Intelligence <ArrowRight size={16} /></Link></aside> }
function Metric({ label, value }: { label: string; value: string }) { return <div><p className="text-xs text-slate-500">{label}</p><p className="mt-1 font-medium text-[#172033]">{value}</p></div> }
